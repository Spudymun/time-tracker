# QA Manual Testing Checklist — Time Tracker

> Роль: Senior QA Engineer  
> Дата: 2026-02-23  
> Тестировщик: GitHub Copilot (AI Agent)  
> Покрытие: все фичи из spec/, бизнес-правила, edge cases, UI states, безопасность, кросс-браузерность

---

## Найденные и исправленные баги

| #   | Приоритет | Описание                                                                                                                                                                                                                                                                                                   | Статус          |
| --- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| B1  | **P1**    | `prisma/schema.prisma`: `TimeEntry.projectId @db.Uuid` несовместим с `Project.id String` (text). FK не создавался → `P2021` при любом запросе. **Фикс**: убрать `@db.Uuid`, пересоздать БД через `db:push --force-reset`.                                                                                  | ✅ Исправлен    |
| B2  | **P1**    | `lib/stores/entries-store.ts` + `timer-store.ts`: `useEntriesActions()` / `useTimerActions()` возвращали новый объект `{}` на каждый рендер без `useShallow`. React 18 `useSyncExternalStore` → бесконечный цикл → главная страница крашилась. **Фикс**: добавить `useShallow` из `zustand/react/shallow`. | ✅ Исправлен    |
| B3  | **P1**    | `components/projects/ProjectsList.tsx`: `handleCreate` отправлял `{ estimatedHours: null, hourlyRate: null }`, но `CreateProjectSchema` ожидает `.optional()` (не `.nullable()`) → Zod `400`. **Фикс**: стрипить null через spread syntax.                                                                 | ✅ Исправлен    |
| B4  | **P2**    | `TimerDisplay`: формат `0:09:10` вместо `00:09:10` — часы не zero-padded при < 10 ч.                                                                                                                                                                                                                       | 🔴 Не исправлен |

---

## Условные обозначения

- `[ ]` — не проверено
- `[x]` — пройдено
- `[!]` — баг найден (добавить описание рядом)
- `[~]` — частично, нужна доработка

Приоритет: **[P1]** — критично, **[P2]** — важно, **[P3]** — желательно

---

## 0. Подготовка окружения

- [x] **[P1]** Приложение запускается: `npm run dev` без ошибок в консоли
- [ ] **[P1]** `npm run build` проходит без ошибок TypeScript
- [ ] **[P1]** `npm run type-check` — 0 ошибок
- [ ] **[P1]** `npm run lint` — 0 ошибок
- [x] **[P2]** База данных подключена, миграции применены (`npm run db:push`) — `--force-reset` из-за B1
- [x] **[P2]** `.env.local` содержит `DATABASE_URL` и `AUTH_SECRET`
- [~] **[P3]** Есть тестовые данные: 2 пользователя ✅, 1 проект ✅, записи в процессе

---

## 1. Аутентификация

### 1.1 Защита маршрутов

- [x] **[P1]** `/` без сессии → редирект на `/login`
- [x] **[P1]** `/projects` без сессии → редирект на `/login?callbackUrl=/projects`
- [ ] **[P1]** `/reports` без сессии → редирект на `/login?callbackUrl=/reports`
- [ ] **[P1]** `/tags` без сессии → редирект на `/login?callbackUrl=/tags`
- [x] **[P1]** После логина → редирект обратно на `callbackUrl`
- [x] **[P1]** API `/api/projects` без токена → `401 Unauthorized` (не редирект HTML)
- [x] **[P1]** API `/api/time-entries` без токена → `401`

### 1.2 Регистрация (email + password)

- [x] **[P1]** Happy path: имя + email + пароль ≥ 8 символов → аккаунт создан, автологин, редирект `/`
- [x] **[P1]** Дублирование email → `409`, inline ошибка в форме
- [x] **[P1]** Пароль < 8 символов → inline ошибка, форма не отправлена
- [ ] **[P1]** Пустой email → inline ошибка
- [ ] **[P1]** Невалидный email (без @) → inline ошибка
- [x] **[P2]** Имя пустое → inline ошибка
- [ ] **[P2]** Имя > 50 символов → inline ошибка
- [ ] **[P2]** Пароль > 72 символов → inline ошибка (bcrypt limit)
- [x] **[P2]** `passwordHash` не возвращается в API responses
- [x] **[P3]** Страница `/register` не содержит TimerBar (auth layout)

### 1.3 Вход (email + password)

- [x] **[P1]** Happy path: корректный email + пароль → JWT сессия → редирект `/`
- [x] **[P1]** Неверный пароль → toast error, форма не сбрасывается (CredentialsSignin в логах)
- [ ] **[P1]** Email не существует → toast error
- [x] **[P2]** Страница `/login` не содержит TimerBar

### 1.4 OAuth (GitHub / Google)

- [ ] **[P1]** Кнопка «Continue with GitHub» → редирект на OAuth provider
- [ ] **[P1]** Первый вход через GitHub → создаётся User + Account, редирект `/`
- [ ] **[P1]** Повторный вход через GitHub → существующий User
- [ ] **[P2]** Email от GitHub совпадает с email Credentials-пользователя → показать `OAuthAccountNotLinked` error
- [ ] **[P2]** Кнопка «Continue with Google» работает аналогично

### 1.5 Выход

- [ ] **[P1]** Клик на аватар/имя в хедере → дропдаун с именем, email, кнопкой «Sign out»
- [ ] **[P1]** «Sign out» → сессия уничтожена → редирект `/login`
- [ ] **[P1]** После выхода: повторный GET `/api/...` возвращает `401`

### 1.6 Изоляция данных между пользователями

- [ ] **[P1]** Пользователь A не видит проекты пользователя B
- [ ] **[P1]** GET `/api/time-entries?...` возвращает только записи текущего пользователя
- [ ] **[P1]** PUT `/api/time-entries/[id_чужой]` → `404` (не `403`)
- [ ] **[P1]** DELETE `/api/projects/[id_чужой]` → `404`

---

## 2. Таймер (TimerBar)

### 2.1 Idle → Running

- [ ] **[P1]** «Start» без описания и проекта → запись создаётся (`description=null`, `projectId=null`)
- [x] **[P1]** «Start» с описанием → запись создаётся с описанием («Testing QA task»)
- [x] **[P1]** После старта кнопка меняется на красную «Stop»
- [x] **[P1]** Счётчик начинает тикать `hh:mm:ss`
- [ ] **[P1]** `startedAt` берётся с сервера (UTC), не с клиента

### 2.2 TimerDisplay

- [x] **[P1]** Счётчик тикает каждую секунду (1s interval)
- [!] **[P1]** Формат `hh:mm:ss` при < 1 часа: `00:23:45` — **БАГ B4**: отображает `0:09:10` (часы без zero-padding)
- [ ] **[P1]** Формат при > 9 часов: `10:23:45`
- [ ] **[P2]** Таймер > 24 часов: отображается `25:23:45` (не переходит в дни)
- [ ] **[P2]** Idle-состояние: отображается `00:00:00`

### 2.3 Running → Idle (Stop)

- [ ] **[P1]** «Stop» → `stoppedAt` выставляется, `durationSeconds` вычисляется
- [ ] **[P1]** После остановки запись появляется в EntriesList без перезагрузки
- [ ] **[P1]** TimerBar возвращается в idle: поля очищаются, счётчик `00:00:00`
- [ ] **[P2]** Toast success «Timer stopped»

### 2.4 Навигация с активным таймером

- [x] **[P1]** Переход на `/projects` — таймер продолжает тикать в хедере
- [ ] **[P1]** Переход на `/reports` — таймер продолжает тикать
- [ ] **[P1]** Переход на `/tags` — таймер продолжает тикать
- [ ] **[P2]** Перезагрузка страницы (F5) — таймер переинициализируется из `/api/time-entries/active`

### 2.5 Параллельный старт (edge case)

- [ ] **[P1]** Уже есть активная «Task A» → нажать «Start» с новым описанием → «Task A» автоматически останавливается, «Task B» запускается
- [ ] **[P2]** Toast info «Previous timer stopped automatically»
- [ ] **[P2]** В EntriesList «Task A» появляется как completed

### 2.6 TaskAutocomplete

- [ ] **[P2]** Ввод 1+ символов → dropdown с уникальными описаниями (case-insensitive)
- [ ] **[P2]** Максимум 20 вариантов в dropdown
- [ ] **[P2]** Навигация стрелками ↑↓
- [ ] **[P2]** Enter выбирает вариант
- [ ] **[P2]** Esc закрывает dropdown
- [ ] **[P2]** Клик по варианту выбирает его

### 2.7 ProjectSelect

- [ ] **[P2]** Список проектов загружается
- [ ] **[P2]** Цветная точка рядом с названием проекта
- [ ] **[P2]** «No project» как первый вариант
- [ ] **[P2]** Архивированные проекты НЕ отображаются в списке

### 2.8 TagSelect

- [ ] **[P2]** Мульти-выбор тегов
- [ ] **[P2]** Поиск по названию в dropdown
- [ ] **[P2]** Выбранные теги отображаются как цветные chips
- [ ] **[P1]** Попытка добавить 11-й тег → кнопка заблокирована, tooltip «Maximum 10 tags»
- [ ] **[P3]** Создание нового тега из dropdown («Create "new-tag"»)

### 2.9 BillableToggle

- [ ] **[P3]** Иконка `$` / checkbox — активна при billable=true
- [ ] **[P3]** Цвет accent при активном состоянии

### 2.10 Сетевые ошибки

- [ ] **[P1]** Сеть упала в момент «Start» → toast error, кнопка возвращается в состояние «Start»
- [ ] **[P1]** Сеть упала в момент «Stop» → toast error, таймер продолжает тикать

---

## 3. Список записей (EntriesList)

### 3.1 Отображение

- [ ] **[P1]** Список загружается при открытии `/`
- [ ] **[P1]** Записи сгруппированы по дате: TODAY, YESTERDAY, `DD MMM YYYY`
- [ ] **[P1]** Внутри даты — подгруппировка по проекту
- [ ] **[P2]** Для каждой группы проекта: суммарное время
- [ ] **[P1]** Активная запись отображается первой (вверху TODAY) с анимированным индикатором
- [ ] **[P2]** Запись без проекта → группа «No project» с серым цветом
- [ ] **[P2]** Пустое описание → отображается «(no description)» серым курсивом
- [ ] **[P2]** Теги: первые 3 chips, остальные «+N more»
- [ ] **[P3]** Клик по «+N more» разворачивает все теги
- [ ] **[P2]** Billable записи — иконка `$`

### 3.2 Редактирование записи

- [ ] **[P1]** Клик на запись (или иконку edit) → inline-форма открывается в строке (не модал!)
- [ ] **[P1]** Изменение описания → «Save» → запись обновляется без перезагрузки
- [ ] **[P1]** Изменение проекта → «Save» → обновляется
- [ ] **[P2]** Изменение тегов → «Save» → обновляется
- [ ] **[P2]** Изменение billable → «Save»
- [ ] **[P1]** Нельзя редактировать активную запись через EntriesList (только через TimerBar)
- [ ] **[P2]** Архивированные проекты НЕ показываются в dropdown при редактировании

### 3.3 Ручная коррекция времени

- [ ] **[P1]** Клик на время записи → inline-инпут с форматом `hh:mm`
- [ ] **[P1]** Enter → `stoppedAt = startedAt + введённые минуты`; `startedAt` не меняется
- [ ] **[P1]** Ввод `00:00` → inline ошибка «Minimum 1 minute», запрос не отправляется
- [ ] **[P2]** Допустимый диапазон: `00:01` – `99:59`
- [ ] **[P2]** Некорректный формат → inline ошибка

### 3.4 Удаление записи

- [ ] **[P1]** Hover на записи → иконка «Удалить»
- [ ] **[P1]** Клик → inline подтверждение (не модал)
- [ ] **[P1]** Подтверждение → запись удаляется из списка
- [ ] **[P2]** Удаление последней записи за день → дата-группа исчезает
- [ ] **[P2]** Toast success «Entry deleted»
- [ ] **[P2]** Нельзя удалить активную запись (кнопка скрыта или задизейблена)

### 3.5 Continue

- [ ] **[P1]** Hover → появляется иконка ▶ «Continue»
- [ ] **[P1]** Клик «Continue» → новая запись с теми же `description`, `projectId`, тегами, `billable`
- [ ] **[P1]** Оригинальная запись НЕ изменяется
- [ ] **[P1]** Если была активная → она автоматически останавливается
- [ ] **[P2]** TimerBar обновляется с данными новой записи

### 3.6 Фильтрация

- [ ] **[P2]** Поиск по описанию (case-insensitive substring) → список обновляется
- [ ] **[P2]** Фильтр по проекту → только записи этого проекта
- [ ] **[P2]** Фильтр по тегу → только записи с этим тегом
- [ ] **[P2]** Фильтр billable=true → только billable записи
- [ ] **[P2]** Комбинирование фильтров AND → работает корректно
- [ ] **[P2]** Кнопка «Clear» → все фильтры сбрасываются

### 3.7 Пагинация

- [ ] **[P2]** По умолчанию загружаются записи за последние 7 дней
- [ ] **[P2]** Кнопка «Показать больше» → загружает ещё 7 дней
- [ ] **[P3]** Более 200 записей → кнопка «Показать больше» видна

---

## 4. Проекты

### 4.1 Список проектов

- [x] **[P1]** `/projects` загружается, отображает список
- [x] **[P1]** Каждый проект: цветная точка, название, общее время, кол-во записей
- [ ] **[P2]** Сортировка по `createdAt DESC` (новые сверху)
- [x] **[P2]** Пустой список → empty state с призывом создать проект

### 4.2 Создание проекта

- [x] **[P1]** Кнопка «New project» → форма открывается inline
- [x] **[P1]** Ввод названия + выбор цвета → «Сохранить» → проект в списке без перезагрузки
- [ ] **[P1]** Дублирование имени → `409`, toast «Project name already exists»
- [ ] **[P1]** Пустое имя → inline ошибка
- [ ] **[P2]** Имя > 50 символов → ошибка
- [ ] **[P2]** ColorPicker: можно ввести HEX вручную; невалидный HEX → ошибка
- [x] **[P2]** Дефолтный цвет `#6366f1`

### 4.3 Редактирование проекта

- [ ] **[P1]** Клик edit → поля редактируемы inline
- [ ] **[P1]** «Save» → обновляется без перезагрузки
- [ ] **[P2]** Переименование в уже существующее имя → `409`

### 4.4 Удаление проекта

- [ ] **[P1]** Клик delete → подтверждение с предупреждением «X time entries will become unassigned»
- [ ] **[P1]** Подтверждение → проект удалён; его записи получают `projectId=null`
- [ ] **[P2]** Записи бывшего проекта в EntriesList показываются как «No project»

### 4.5 Архивация проекта

- [ ] **[P1]** «Архивировать» → `isArchived=true`
- [ ] **[P1]** Архивированный проект исчезает из dropdown в TimerBar
- [ ] **[P1]** Архивированный проект исчезает из dropdown в EntryItem edit mode
- [ ] **[P1]** В `/projects`: архивные скрыты по умолчанию, toggle «Показать архивные»
- [ ] **[P2]** Архивированные записи по-прежнему видны в EntriesList и Reports
- [ ] **[P2]** Нельзя создать проект с именем уже существующего архивного
- [ ] **[P2]** «Разархивировать» → `isArchived=false`, проект снова в dropdown

### 4.6 Estimates (бюджет времени)

- [ ] **[P2]** `estimatedHours` задан → прогресс-бар `totalSeconds / (estimatedHours * 3600)`
- [ ] **[P2]** 0–79% → зелёный прогресс-бар
- [ ] **[P2]** 80–99% → жёлтый прогресс-бар
- [ ] **[P2]** ≥ 100% → красный прогресс-бар
- [ ] **[P2]** Без `estimatedHours` → прогресс-бар не отображается
- [ ] **[P3]** `estimatedHours = 0` → `400` ошибка

### 4.7 Hourly Rate

- [ ] **[P3]** `hourlyRate` задан → в списке проектов колонка «Доход»
- [ ] **[P3]** Earnings = billableSeconds / 3600 \* hourlyRate (только billable!)
- [ ] **[P3]** `hourlyRate < 0` → `400`

---

## 5. Теги

### 5.1 Список тегов

- [ ] **[P1]** `/tags` загружается
- [ ] **[P1]** Каждый тег: цвет, название, количество использований (`usageCount`)
- [ ] **[P2]** Пустой список → empty state

### 5.2 Создание тега

- [ ] **[P1]** Название + цвет → «Create» → тег в списке
- [ ] **[P1]** Дублирование имени (case-insensitive) → `409` toast
- [ ] **[P1]** Пустое имя → inline ошибка
- [ ] **[P2]** Имя > 30 символов → ошибка
- [ ] **[P2]** Имя сохраняется в lowercase
- [ ] **[P2]** Дефолтный цвет `#10b981`

### 5.3 Редактирование и удаление тега

- [ ] **[P1]** Edit → изменить название/цвет → «Save»
- [ ] **[P1]** Delete используемого тега → предупреждение «Used in X entries»
- [ ] **[P1]** Подтверждение удаления → тег удалён, связи с TimeEntry удалены (Cascade)
- [ ] **[P2]** Записи после удаления тега: тег пропадает из chips

---

## 6. Отчёты

### 6.1 Просмотр отчёта

- [ ] **[P1]** `/reports` загружается
- [ ] **[P1]** Быстрые периоды: Today, This week, This month
- [ ] **[P1]** Таблица «By Projects»: название, billable ч., not-billable ч., итого, %
- [ ] **[P1]** Строка «Total» в конце
- [ ] **[P2]** «By Tags» вкладка: аналогично по тегам
- [ ] **[P2]** Раскрытие деталей по клику на строку проекта
- [ ] **[P2]** Пустой период → «No data for this period»
- [ ] **[P2]** Активная запись НЕ включается в отчёт

### 6.2 Custom Range

- [ ] **[P2]** «Custom range» → date range picker (from + to)
- [ ] **[P2]** `to` не может быть раньше `from` — UI блокирует выбор
- [ ] **[P1]** `from > to` в API → `400`
- [ ] **[P2]** Период > 366 дней → `400`

### 6.3 Earnings в отчёте

- [ ] **[P3]** Проект с `hourlyRate` → колонка «Заработано»: billableSeconds / 3600 \* hourlyRate
- [ ] **[P3]** Строка Total включает суммарный заработок

### 6.4 CSV Export

- [ ] **[P1]** Кнопка «Export CSV» → браузер скачивает файл
- [ ] **[P1]** Имя файла: `time-report-YYYY-MM-DD_YYYY-MM-DD.csv`
- [ ] **[P1]** Заголовок CSV: `Date,Start,Stop,Duration (h),Project,Description,Tags,Billable`
- [ ] **[P1]** Каждая строка = одна TimeEntry (детально, не агрегация)
- [ ] **[P2]** 0 записей → файл с только заголовком (не пустой файл)
- [ ] **[P2]** Несколько тегов: `"dev,bug"` в кавычках
- [ ] **[P2]** `Content-Disposition: attachment` в заголовке ответа

---

## 7. Dashboard

### 7.1 Виджет на главной

- [ ] **[P1]** Dashboard widget отображается на `/` (под хедером, над EntriesList)
- [ ] **[P1]** Заголовок: «Week of Feb 16–22» (текущая неделя Пн–Вс)
- [ ] **[P1]** Bar chart: 7 столбцов (Пн–Вс), высота = часы
- [ ] **[P2]** Stacked bars по проектам с цветами из Project.color
- [ ] **[P2]** Суммарно: total + billable за неделю
- [ ] **[P2]** Top 5 projects список
- [ ] **[P2]** Ссылка «→ View full report» ведёт на `/reports` с текущим периодом

### 7.2 Навигация по неделям

- [ ] **[P2]** Кнопки «‹» «›» переключают неделю
- [ ] **[P2]** Кнопка «›» заблокирована на текущей неделе (нельзя вперёд)
- [ ] **[P2]** Переключение обновляет данные

### 7.3 Пустая неделя

- [ ] **[P2]** Нет записей → 7 пустых баров + плейсхолдер «No entries this week...»
- [ ] **[P2]** TopProjectsList скрыт

### 7.4 Weekly Target

- [ ] **[P3]** Настройка цели (localStorage) → прогресс-бар
- [ ] **[P3]** < цели → зелёный
- [ ] **[P3]** ≥ цели → синий (переработка)
- [ ] **[P3]** `weeklyTargetHours` в localStorage не найден → бар скрыт
- [ ] **[P3]** Нет гидратационной ошибки (SSR vs localStorage)

### 7.5 Earnings summary

- [ ] **[P3]** Хотя бы один проект с `hourlyRate` → строка «Заработано: $X»

---

## 8. UI States

### 8.1 Loading States

- [ ] **[P2]** `/` пока загружается → skeleton для Dashboard и EntriesList
- [ ] **[P2]** `/projects` пока загружается → skeleton строк
- [ ] **[P2]** `/reports` пока загружается → skeleton таблицы
- [ ] **[P2]** `/login` и `/register` пока загружаются → Spinner по центру
- [ ] **[P2]** Нет текста «Loading...» — только `<Spinner />`

### 8.2 Empty States

- [ ] **[P2]** EntriesList без записей → сообщение «Start tracking to see your entries»
- [ ] **[P2]** Projects без проектов → призыв создать первый
- [ ] **[P2]** Tags без тегов → призыв создать первый

### 8.3 Error States

- [ ] **[P1]** Несуществующий маршрут → `not-found.tsx`: «Page not found» + кнопка «Go to Dashboard»
- [ ] **[P1]** Необработанная JS-ошибка в Server Component → `error.tsx`: «Something went wrong» + «Try again» и «Go to Dashboard»
- [ ] **[P2]** `error.tsx` — `"use client"` компонент (Next.js требование)

### 8.4 Toast-уведомления

- [ ] **[P1]** Success toast: зелёный, автозакрытие через 3 сек
- [ ] **[P1]** Error toast: красный, автозакрытие через 5 сек
- [ ] **[P2]** Info toast: нейтральный, 3 сек
- [ ] **[P2]** Максимум 3 одновременных toast
- [ ] **[P2]** НЕТ toast при 404 (тихая обработка)
- [ ] **[P2]** НЕТ toast при 401 → только редирект на `/login`
- [ ] **[P2]** НЕТ toast при валидационных ошибках формы (inline сообщение)

### 8.5 401 Interceptor

- [ ] **[P1]** Zustand store получает `401` от API → редирект на `/login`
- [ ] **[P2]** Сессия истекла в середине работы → при следующем запросе редирект `/login`

---

## 9. Дизайн-система

### 9.1 Цветовые токены

- [ ] **[P3]** Нет классов `bg-indigo-*`, `text-slate-*`, `bg-gray-*` — только семантические токены
- [ ] **[P3]** Текст: `text-text-1` / `text-text-2` / `text-text-3`
- [ ] **[P3]** Фон: `bg-bg` / `bg-surface` / `bg-surface-2`
- [ ] **[P3]** Акцент: `bg-primary` / `text-primary`

### 9.2 Интерактивность

- [ ] **[P3]** Все кнопки: `hover:` + `focus-visible:ring-2 focus-visible:ring-primary`
- [ ] **[P3]** Disabled-кнопки: `disabled:opacity-50`
- [ ] **[P3]** TimerDisplay использует класс `timer-display`
- [ ] **[P3]** Нет `style={{}}` (кроме динамических цветов через `backgroundColor`)

### 9.3 Компоненты

- [ ] **[P3]** Нет нативных `<button>` и `<input>` вне `components/ui/`
- [ ] **[P3]** Модалы только через `components/ui/Modal`

---

## 10. Бизнес-правила и API

### 10.1 UUID / ID

- [ ] **[P2]** Все ID — UUID-формат (не sequential integers)

### 10.2 Даты

- [ ] **[P2]** Даты в БД в UTC
- [ ] **[P2]** `createdAt` / `updatedAt` не принимаются от клиента

### 10.3 HTTP-коды

- [ ] **[P1]** POST создание → `201`
- [ ] **[P1]** GET / PUT → `200`
- [ ] **[P1]** DELETE → `204`
- [ ] **[P1]** Невалидные данные → `400` с `{ error, details? }`
- [ ] **[P1]** Ресурс не найден → `404`
- [ ] **[P1]** Конфликт → `409`
- [ ] **[P2]** Серверная ошибка → `500` без stack trace в ответе

### 10.4 Валидация

- [ ] **[P1]** POST/PUT без body → `400`
- [ ] **[P1]** Пустая строка в nullable поле нормализуется в `null`
- [ ] **[P1]** `id` в URL имеет приоритет над `id` в body

### 10.5 Одна активная запись

- [ ] **[P1]** Нельзя создать второй активный таймер (API автоматически останавливает предыдущий)
- [ ] **[P1]** Нельзя удалить активную запись → `400`

---

## 11. Безопасность

- [ ] **[P1]** `passwordHash` не возвращается ни в каком API response
- [ ] **[P1]** Stack trace не возвращается клиенту в ответах `500`
- [ ] **[P1]** `.env.local` не попадает в репозиторий (проверить `.gitignore`)
- [ ] **[P1]** `AUTH_SECRET` длиннее 32 символов
- [ ] **[P2]** SQL инъекции: нет `$queryRaw` без параметризации в коде
- [ ] **[P2]** Нет `any` типов в TypeScript коде

---

## 12. Кросс-браузерность и устройства

- [ ] **[P2]** Chrome (последняя версия) — полная функциональность
- [ ] **[P2]** Firefox — полная функциональность
- [ ] **[P3]** Safari — полная функциональность
- [ ] **[P2]** Mobile 375px: TimerBar не ломает layout
- [ ] **[P2]** Mobile 375px: списки читаемы
- [ ] **[P3]** Планшет 768px: layout корректный

---

## 13. Производительность

- [ ] **[P3]** Lighthouse Score >= 85 (Performance) на главной
- [ ] **[P3]** LCP < 2.5s на `localhost` после build
- [ ] **[P3]** Нет memory leak: открыть app на 5 мин, проверить что `setInterval` очищается при unmount

---

## 14. Регрессионные сценарии (связи между фичами)

- [ ] **[P1]** Создать проект → назначить при старте таймера → остановить → запись в EntriesList с проектом
- [ ] **[P1]** Создать тег → назначить к записи → удалить тег → тег пропадает с записи, запись жива
- [ ] **[P1]** Удалить проект → его записи в EntriesList → «No project»
- [ ] **[P1]** Запустить таймер → перейти на `/reports` → отчёт не включает активную запись
- [ ] **[P1]** Запустить, остановить → перейти на `/reports` → запись есть в отчёте и в CSV
- [ ] **[P2]** Создать 10 записей за неделю → Dashboard показывает корректный stacked bar
- [ ] **[P2]** Архивировать проект с активным таймером → таймер работает → запись в отчёте видна
- [ ] **[P2]** Continue на записи → новая запись в TimerBar с теми же данными → Stop → в EntriesList отдельная строка

---

## Итоговая сводка

| Область       | Всего    | P1       | P2       | P3      |
| ------------- | -------- | -------- | -------- | ------- |
| Auth          | ~30      | 18       | 9        | 3       |
| Timer         | ~35      | 12       | 17       | 6       |
| Entries       | ~33      | 12       | 18       | 3       |
| Projects      | ~30      | 12       | 14       | 4       |
| Tags          | ~15      | 6        | 7        | 2       |
| Reports       | ~20      | 9        | 9        | 5       |
| Dashboard     | ~18      | 4        | 8        | 6       |
| UI States     | ~20      | 6        | 13       | 1       |
| Design        | ~10      | 0        | 0        | 10      |
| API/Rules     | ~15      | 10       | 4        | 1       |
| Security      | ~7       | 5        | 2        | 0       |
| Cross-browser | ~7       | 0        | 4        | 3       |
| Regression    | ~12      | 7        | 5        | 0       |
| **Итого**     | **~252** | **~101** | **~110** | **~44** |
