---
name: spec-driven
description: >
  Spec-driven development workflow. Use when implementing any new feature.
  Guides agent through: load spec → plan → implement → verify.
  Ensures business rules and edge cases are respected before code is written.
user-invokable: true
argument-hint: "[feature-name]"
---

# Spec-Driven Development Skill

## Когда использовать

Используй эту skill для **любой новой фичи** или значимого изменения.
Запускается агентом автоматически при запросах типа: "реализуй", "добавь", "создай".

## Workflow

### Фаза 1: Загрузи контекст (ОБЯЗАТЕЛЬНО перед кодом)

```
1. read_feature({name}) через MCP  ← если MCP сервер запущен
   ИЛИ
   #file:spec/FEATURE_{name}.md    ← если MCP недоступен

2. #file:spec/DOMAIN.md
3. #file:spec/BUSINESS_RULES.md
```

Если spec не найден → **стоп**: "Создай spec/FEATURE\_{name}.md перед продолжением"

### Фаза 2: Изучи кодовую базу

Перед созданием нового файла — найти существующее:

- Похожие репозитории в `lib/db/`
- Похожие схемы в `lib/validations/`
- Похожие компоненты в `components/`
- Паттерны роутов в `app/api/`

Правило: если 80%+ покрыто — переиспользовать. Если 50-80% — адаптировать. Если < 50% — создать.

### Фаза 3: Порядок реализации

Всегда в таком порядке (dependency order):

1. Prisma schema (если новая сущность)
2. Zod validation schema
3. Repository (DB layer)
4. Service (business logic, если есть)
5. API routes
6. Zustand store (если нужен client state)
7. React components
8. Tests для сервисов и схем

### Фаза 4: Верификация

После каждого шага:

- Проверь TypeScript: нет `any`, все типы явные
- API routes: Zod-валидация ДО бизнес-логики
- Тесты написаны и описывают поведение (не impl. details)

### Фаза 5: Интеграция

- Компоненты подключены к страницам
- Роуты зарегистрированы
- Нет orphaned кода (файлы созданы но нигде не используются)

## Финальный отчёт агента

После завершения:

```
✅ Реализовано: [список созданных файлов]
🔗 Интегрировано в: [страницы/компоненты]
🧪 Тесты: [что покрыто]
🔍 Проверь вручную:
  - [конкретный сценарий 1]
  - [edge case из spec]
```
