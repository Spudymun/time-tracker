# Feature: [FEATURE_NAME]

> Создан: [ДАТА]
> Статус: Draft / Ready / In Progress / Done

## Обзор

**Актор:** [Кто использует фичу]
**Цель:** [Что достигает пользователь]
**MVP приоритет:** Must Have / Should Have / Could Have

## Пользовательские сценарии

### Happy Path

1. Пользователь делает [действие]
2. Система [реагирует]
3. Пользователь видит [результат]

### Error Cases

- Если [условие] → [что показывать пользователю]
- Если [условие] → [что показывать пользователю]

## Бизнес-правила

- [Правило 1: конкретное и проверяемое]
- [Правило 2]
- [Правило 3]

## Edge Cases

- [Edge case 1: например "Что если пользователь кликает дважды быстро"]
- [Edge case 2]
- [Edge case 3]

## State Machine (если применимо)

```
initial → [state1] → [state2] → final
              ↑                    |
              └────────────────────┘ (reset)
```

| Состояние | Разрешённые переходы | Запрещённые действия |
| --------- | -------------------- | -------------------- |
| [state1]  | → state2             | [что нельзя]         |

## API

| Метод  | Endpoint             | Описание        |
| ------ | -------------------- | --------------- |
| POST   | /api/[resource]      | Создать         |
| GET    | /api/[resource]      | Получить список |
| GET    | /api/[resource]/[id] | Получить одну   |
| PUT    | /api/[resource]/[id] | Обновить        |
| DELETE | /api/[resource]/[id] | Удалить         |

### Request Schema

```ts
{
  field: string  // описание
  optionalField?: string
}
```

### Response Schema

```ts
{
  id: string;
  field: string;
  createdAt: string; // ISO 8601
}
```

## UI

### Компоненты

- `[ComponentName]` — [описание]
- `[ComponentName]` — [описание]

### Страница

- Route: `/[path]`
- Тип: Server Component / Client Component
- Зависимости: [список других фич/компонентов]

## Технические заметки

- [Заметка 1: например "Использовать debounce 300ms для search"]
- [Заметка 2]

## Критерии готовности (Definition of Done)

- [ ] API route реализован и возвращает правильные статус-коды
- [ ] Zod-валидация покрывает все поля
- [ ] Бизнес-правила применяются
- [ ] Edge cases обработаны
- [ ] UI компоненты работают
- [ ] Тесты для сервисов и схем написаны
- [ ] Функциональность проверена вручную в dev
