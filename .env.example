DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Auth.js: сгенерировать командой: openssl rand -base64 32
AUTH_SECRET="your-secret-here-min-32-chars"

# GitHub OAuth (опционально — для GitHub Sign In)
# https://github.com/settings/developers → New OAuth App
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# Google OAuth (опционально — для Google Sign In)
# https://console.cloud.google.com → Credentials → OAuth 2.0
AUTH_GOOGLE_CLIENT_ID=""
AUTH_GOOGLE_CLIENT_SECRET=""
